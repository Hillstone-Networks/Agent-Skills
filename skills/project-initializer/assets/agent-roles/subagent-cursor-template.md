---
name: role-id-kebab-case
description: 具体描述该子代理的职责；明确「何时委派给该子代理」——例如：当需要实现或修改后端 API、数据模型与领域逻辑时委派。
# model: inherit   # 可选：fast | inherit | 模型 ID
# readonly: false  # 可选：true 时受限写入
# background: false # 可选：true 时后台运行
---

# 角色名称（来自 displayName）

## 职责与边界

（由 role 的 scope 转写）

## 期望输入

（由 inputExpectation 转写：上游角色、产出物、格式要求）

## 必须遵守的约束

（由 constraints 转写，列表）

## 产出物与质量标准

（由 outputSpec 转写：产出物名称/格式、验收标准、可选模板引用）

## 交接说明

（由 handoff 转写：交付给谁、形式、交接前检查项）
